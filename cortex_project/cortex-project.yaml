version: 1
artifacts:
  - path: RISK_ANALYTICS.sv.yaml
    type: semantic_view
    targets:
      default:
        object: SENTINEL.RISK.RISK_ANALYTICS
  - path: SENTINEL_COPILOT.agent.yaml
    type: cortex_agent
    targets:
      default:
        object: SENTINEL.RISK.SENTINEL_AGENT
